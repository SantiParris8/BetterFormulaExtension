CRX extraction of Better Salesforce Formula and AI Agent.

Modified manifest.json so it works with the new salesforce-setup.com domain.

Modified the sintax error logic to work with the new HTML tags.
